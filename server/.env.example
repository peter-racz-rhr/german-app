GROQ_API_KEY=gsk_your-key-here
PORT=8787

# Optional — set both to persist data in Upstash Redis (free, survives cloud redeploys).
# Without these, data is saved to local JSON files in server/data (lost on redeploy).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
